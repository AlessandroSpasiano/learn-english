spring:
  jpa:
    database: h2
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
  datasource:
    url: jdbc:h2:mem:testdb;
    username: sa
    password:

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: true